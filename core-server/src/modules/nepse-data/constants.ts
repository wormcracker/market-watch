import path from "path";

export const CACHE_FILE = path.join(process.cwd(), "data", "nepse", "market-snapshot.json");
export const CONFIG_FILE = path.join(process.cwd(), "config", "nepse", "settings.json");
